import React from "react";

export const metadata = {
  title: "Minhazul Abedin | Blog",
  description: "Portfolio",
};

const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
