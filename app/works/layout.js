import React from "react";

export const metadata = {
  title: "Minhazul Abedin | Works",
  description: "Portfolio",
};

const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
